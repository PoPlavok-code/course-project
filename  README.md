## Обработка исключений (Домашнее задание: Обработка исключений)

- **Валидация количества**: При создании товара с нулевым количеством выбрасывается исключение `ValueError` с сообщением "Товар с нулевым количеством не может быть добавлен".
- **Средний ценник категории**: Реализован метод `get_average_price()` в классе `Category`, который подсчитывает средний ценник всех товаров. При пустой категории метод возвращает 0 (обработка `ZeroDivisionError`).
- **Тестирование**: Написаны тесты для проверки новой функциональности (23 теста, все проходят).